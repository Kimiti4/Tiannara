program: TIA_FORENSIC_RECON_ENGINE
move: PROVENANCE-CLOSURE-EVIDENCE-ARCHITECTURE-GATE
schema_version: "1.0.0"
method: "Read-only reconnaissance of the T0 tree (git ls-tree inventory capped reads; targeted grep/read of provenance, execution, certification, contract, and metric code and artifacts) plus design synthesis. No implementation, no test execution, no commits, no pushes."
STATUS: "PARTIAL PASS (architecture sufficiently characterized and a coherent minimum design is defined; important evidence gaps remain; see GATE)"

scope:
  - "Source identity, contract identity, execution identity, result provenance, metric provenance, certificate provenance, certificate self-hashing across the T0 corpus"
  - "Code examined: lib/tiannara/evidence/provenance.ex (canonical provenance schema), lib/tiannara/phase4/real_execution.ex (execution ledger + provenance build), lib/tiannara/phase4/physics_pilot_execution.ex, lib/tiannara/os/governance/certification/laboratory.ex (gc_009/gc_010 metric computation), lib/tiannara/os/governance/certification/independent_auditor.ex (independent recomputation), lib/tiannara/os/governance/long_horizon_replay.ex (gc_012 metric computation), lib/mix/tasks/tiannara.generate_minimal_repro_package.ex (MOCK-HASH generator)"
  - "Artifacts examined: phase14/certification/certificate.json, PHASE14_GOVERNANCE_CERTIFICATE.json, PHASE14_GOVERNANCE_CERTIFICATE_FINAL.json, PHASE14_PARTIAL_CERTIFICATE.json, phase14 campaign_results gc_001..gc_012, phase14 hashes.json + *.sha256, manifests (master_manifest, manifest, evidence_index), runtime_freeze.json, independent_audit_report.json, certification_certificate.json, data/minimal_repro_package/constitution_certificate.json, docs/RUNTIME_FREEZE_CERTIFICATE.json, evidence/certificates/cert-entropy-003.json, archive run packets + meta (2026-Q2), priv real_execution/executions.jsonl, remediation contracts (priv/tiannara/remediation/contracts/AC001/ MC00*/AC001_*.contract.yaml), probe contracts (docs/probes), tiannara_runtime CPL schemas, test suite (test/tiannara/certification/certification_pipeline_test.exs, test/tiannara/os/governance/integration_test.exs, provenance tests)"
  - "Boundary: reconnaissance + design only; T0 preserved; no execution of any experiment"

T0: "PRESERVED 9753a6d08702064f4d00395a1bec1c4bf3887381 / tree 06f06e5e91f33e75437a47ca901eaa7e97bb0edd; working tree 3,366 dirty entries untouched; no commits or pushes"

objects_examined:
  provenance_primitives:
    - "Tiannara.Evidence.Provenance (lib/tiannara/evidence/provenance.ex): kind/source/producer/produced_at/execution_id/experiment_id/environment/evidence_hash/audit_trail; requires execution_id for :real_execution; acceptable_as_evidence? only :real_execution and :imported_evidence(with source); hash via :erlang.term_to_binary"
    - "Existing model has NO source_identity (repo/commit/tree), NO contract_identity, NO input/output manifest, NO result_identity, NO metric_manifest, NO environment_identity object"
    - "Provenance records are built in-memory per execution; the execution ledger is a separate append-only JSONL (priv/tiannara/real_execution/executions.jsonl); no evidence found of Provenance.build records being persisted alongside certificates"
  source_identity:
    - "git repo/commit/tree identity ABSENT from every execution record examined (real_execution ledger, physics executions.jsonl, archive run meta/packets)"
    - "Archive run config_hash is 7a3244ead97d (12 hex chars; prefix-form, semantics unspecified, not a git object id); seed 12345 present; run_id full timestamp+hash+machine name present"
    - "T0 commit hash known only a posteriori (the audit snapshot); no execution references it"
    - "Working-tree state identity ABSENT (no dirty-tree fingerprint exists anywhere)"
  contract_identity:
    - "Contracts EXIST as YAML (remediation AC001_*/MC00*/... contract files: gate_id, parent_gate, campaign, class, mode, created_at, bounded_proposition, requires, verifies, forbids, verdict_space, certification_boundary, constitutional_basis, contract_hash: null)"
    - "contract_hash is null in the FINAL certification closure contract; no hashed contract binds to any execution or certificate"
    - "Probe contracts (docs/probes U1x-R2 etc.) and runtime CPL schemas (tiannara_runtime/priv/certification/cpl/schemas) exist but are not referenced by certificates"
    - "versioning is filename/gate_id based; no contract version field inside contracts"
  execution_identity:
    - "real_execution ledger fields: type, execution_id (exec_<unix>_<unique_integer>), experiment_id, executed_at, verification {harness :real_sandbox, method, backend, baseline, verdict, tests{all_passed, details}, benchmark}"
    - "execution_id PRESENT; experiment_id PRESENT; start_time PRESENT (executed_at); end_time ABSENT; seed ABSENT; runtime_version ABSENT (static 0.1.0 strings elsewhere); source_identity ABSENT; contract_identity ABSENT; input_manifest ABSENT; output_manifest ABSENT; failure info PARTIAL (verdict/tests)"
    - "archive runs: run_id, seed 12345, config_hash, per-step telemetry PRESENT; environment identity ABSENT"
    - "physics executions.jsonl: ids + full I/O config + timestamps PRESENT; seed/source-version/campaign ABSENT"
  result_provenance:
    - "campaign result files (gc_*) carry campaign names and metric blocks ONLY; NO execution_id, run_id, or result artifact identity"
    - "archive packets bind run_meta internally (run_id/seed/config_hash); no result outside the packet references them"
    - "physics execution rows carry ids and I/O but no downstream result references them"
    - "RESULT -> EXECUTION: NOT ESTABLISHED (no explicit immutable identifier in either direction)"
  metric_provenance:
    - "gc_009 entropy stability: code computes 1000 samples, mean/variance/std_dev (Float.round 4), stable iff std_dev < 0.05; confidence 0.99 and statistical_power 0.98 are SELF-ASSERTED constants, not computed; no metric calculation version; measurements ephemeral (not persisted)"
    - "gc_010 fitness stability: code computes 1000 samples, mean_fitness; stable iff std_dev < 0.05 and mean_fitness >= 0.8; same constant self-assertions"
    - "independent_auditor gc_009 uses 100 samples and only std_dev gate; gc_010 uses 100 samples and only mean >= 0.8 gate - PROTOCOL DIFFERS from certification laboratory (1000 samples + joint gates): metric definition is not identical between certification and independent audit"
    - "gc_012 long-horizon replay: 10k/100k/1M iterations, entropy/fitness measurement series, per-report sha256; result file final_fitness 0.8035; no persisted measurement corpus in repo"
    - "No metric manifest, no aggregation-method record (beyond inline code), no rounding/normalization spec, no input field list, no recomputation capability record"
  certificate_provenance:
    - "certificate.json (12 campaign blocks): source ABSENT, contract ABSENT, execution ABSENT, result PARTIAL (values inline, no artifact refs), metric PARTIAL (values inline, no derivation ref), auditor identity ABSENT, generator version ABSENT (version only in master_manifest: 14.0.999); stored certificate_sha256 b69fee3446.../manifest_sha256 ffb09a90... present (self-hash verified genuine in prior gate)"
    - "PHASE14_GOVERNANCE_CERTIFICATE.json: same class; self-hash stored 409dbc2a... vs recomputed 0b1a6ab6... MISMATCH (T0-only artifact, intrinsic)"
    - "PHASE14_GOVERNANCE_CERTIFICATE_FINAL.json: asserts evidence_closure reconstruction_successful/certificate_hash_match/manifest_hash_match true; stated certificate_sha256 c5896ae4... vs byte-exact whole-file recompute 985e0073... (canonical-subset semantics unknown; do not assert mismatch beyond method note)"
    - "certification_certificate.json (meta-certificate): self-named authority, second-order claims, no external anchor"
    - "data/minimal_repro_package/constitution_certificate.json: execution_id MINIMAL-REPRO-2026-07-01T23:56:20 + certificate_id CERT-5850d111, self-minted via MOCK-HASH generator path; replay_status not_run"
    - "docs/RUNTIME_FREEZE_CERTIFICATE.json: generic runtime component list, no bindings"
    - "cert-entropy-003.json (evidence/certificates): type entropy_stability, campaign gc_009 - no execution identity"
    - "PHASE14_PARTIAL_CERTIFICATE.json: version 14.0.99, sha256 365d6c75..., campaigns_executed 2, passed 2, pending 10"
    - "IN EVERY CERTIFICATE CLASS: source commit/tree, contract version/hash, test-suite version, execution/run/campaign id, input manifest, seed, result artifact ref, metric derivation, and auditor identity are ABSENT or self-referential"

edges_examined:
  - "SOURCE STATE -> CONTRACT VERSION: NOT ESTABLISHED (neither side carries the other; contract_hash null)"
  - "CONTRACT VERSION -> TEST/EXPERIMENT DEFINITION: PARTIALLY ESTABLISHED (contracts and definitions coexist as prose; no identifier link)"
  - "TEST/EXPERIMENT DEFINITION -> EXECUTION RECORD: PARTIALLY ESTABLISHED (experiment_id links some specs to ledger rows; recipe hash ABSENT)"
  - "EXECUTION RECORD -> INPUT MANIFEST: NOT ESTABLISHED (no input manifest exists; config_hash prefix-only)"
  - "INPUT MANIFEST -> EXECUTION OUTPUT: NOT ESTABLISHED (outputs not inventoried against inputs)"
  - "EXECUTION OUTPUT -> RESULT DERIVATION: PARTIALLY ESTABLISHED (archive packet telemetry -> metric blocks exist for pros runs only; no certificate references them)"
  - "RESULT DERIVATION -> METRIC DERIVATION: PARTIALLY ESTABLISHED (metrics computed from measurements within campaign runs; calculation unversioned, thresholds in code, measurements not persisted)"
  - "METRIC DERIVATION -> CERTIFICATION DECISION: PARTIALLY ESTABLISHED (certificate blocks embed metrics; decision authority self-asserted; threshold logic replayable only by code re-run)"
  - "CERTIFICATION DECISION -> CERTIFICATE: PARTIALLY ESTABLISHED (certificate is the decision artifact; decision rationale/authority not independently anchored)"
  - "cross-cutting: EXECUTION -> RESULT -> METRIC -> CERTIFICATE full chain NOT ESTABLISHED for any certificate class"

provenance_matrix:
  - certificate: "certificate.json (phase14)"
    source: "ABSENT"
    contract: "ABSENT"
    execution: "ABSENT"
    result: "PARTIAL (inline values only)"
    metric: "PARTIAL (inline values, no derivation ref)"
    auditor: "ABSENT"
    reproducible: "PARTIAL (protocol in master_manifest; regeneration = re-run; no pinned corpus)"
  - certificate: "PHASE14_GOVERNANCE_CERTIFICATE.json"
    source: "ABSENT"
    contract: "ABSENT"
    execution: "ABSENT"
    result: "PARTIAL"
    metric: "PARTIAL"
    auditor: "ABSENT"
    reproducible: "NO (self-hash mismatch 409dbc2a vs 0b1a6ab6 reveals no canonical procedure)"
  - certificate: "PHASE14_GOVERNANCE_CERTIFICATE_FINAL.json"
    source: "ABSENT"
    contract: "ABSENT"
    execution: "ABSENT"
    result: "PARTIAL"
    metric: "PARTIAL"
    auditor: "ABSENT"
    reproducible: "UNKNOWN (stated c5896ae4 vs whole-file 985e0073; canonical subset semantics undefined)"
  - certificate: "certification_certificate.json (meta)"
    source: "ABSENT"
    contract: "ABSENT"
    execution: "ABSENT"
    result: "PARTIAL"
    metric: "ABSENT"
    auditor: "ABSENT"
    reproducible: "NO (self-named authority, unanchored)"
  - certificate: "constitution_certificate.json (minimal-repro)"
    source: "ABSENT"
    contract: "ABSENT"
    execution: "PARTIAL (self-minted execution_id + certificate_id; MOCK-HASH path; replay not_run)"
    result: "PARTIAL (self-asserted inline)"
    metric: "PARTIAL (inline, no source)"
    auditor: "ABSENT"
    reproducible: "NO (no independent execution record)"
  - certificate: "RUNTIME_FREEZE_CERTIFICATE.json"
    source: "ABSENT"
    contract: "ABSENT"
    execution: "ABSENT"
    result: "ABSENT"
    metric: "ABSENT"
    auditor: "ABSENT"
    reproducible: "N/A (planning vocabulary)"
  - certificate: "PHASE14_PARTIAL_CERTIFICATE.json"
    source: "ABSENT"
    contract: "ABSENT"
    execution: "PARTIAL (gc_001_replay results inline)"
    result: "PARTIAL"
    metric: "PARTIAL"
    auditor: "ABSENT"
    reproducible: "PARTIAL"
  - certificate: "cert-entropy-003.json (evidence certificate)"
    source: "ABSENT"
    contract: "ABSENT"
    execution: "ABSENT"
    result: "PARTIAL"
    metric: "PARTIAL"
    auditor: "ABSENT"
    reproducible: "PARTIAL"

failure_modes:
  - wrong_source_commit: "DETECT: NO (nothing records source); PREVENT: NO; RECOVER: NO"
  - wrong_contract: "DETECT: NO (contract_hash null); PREVENT: NO; RECOVER: NO"
  - wrong_test_suite: "DETECT: NO; PREVENT: NO; RECOVER: NO"
  - wrong_seed: "DETECT: PARTIAL (seed 12345 only in archive packets; not in certificates); PREVENT: NO; RECOVER: NO"
  - wrong_configuration: "DETECT: PARTIAL (config_hash prefix in packets, unbound); PREVENT: NO; RECOVER: NO"
  - wrong_result_file: "DETECT: NO (bundles are path-based); PREVENT: NO; RECOVER: NO"
  - wrong_metric_implementation: "DETECT: NO (no metric version); PREVENT: NO; RECOVER: NO"
  - stale_metric: "DETECT: NO; PREVENT: NO; RECOVER: NO"
  - certificate_copied_from_another_run: "DETECT: NO (no execution binding); PREVENT: NO; RECOVER: NO"
  - certificate_generated_without_execution: "DETECT: NO (manifest campaigns_executed 0 coexists with certificate blocks); PREVENT: NO; RECOVER: NO"
  - execution_record_deleted: "DETECT: NO; PREVENT: NO; RECOVER: NO (absence is indistinguishable from non-existence)"
  - result_record_deleted: "DETECT: NO; PREVENT: NO; RECOVER: NO"
  - partial_execution: "DETECT: PARTIAL (verdict/tests in ledger); PREVENT: PARTIAL (Candidate lifecycle); RECOVER: NO"
  - failed_execution_reported_as_success: "DETECT: PARTIAL (verdict field present but self-asserted); PREVENT: NO; RECOVER: NO"
  - hash_valid_but_semantically_wrong_artifact: "DETECT: NO (self-hash proves bytes only, prior gate finding); PREVENT: NO; RECOVER: NO"
  - duplicate_execution_id: "DETECT: NO (ids are unix+unique_integer; no uniqueness index on ledger); PREVENT: NO; RECOVER: NO"
  - collision: "DETECT: NO; PREVENT: NO; RECOVER: NO"
  - tampered_provenance_record: "DETECT: PARTIAL (ledger append-only JSONL; no chain-of-custody hash); PREVENT: NO; RECOVER: NO"
  - orphaned_certificate: "DETECT: NO; PREVENT: NO; RECOVER: NO"
  - orphaned_result: "DETECT: NO; PREVENT: NO; RECOVER: NO"
  - orphaned_execution: "DETECT: NO; PREVENT: NO; RECOVER: NO"

contradictions:
  - "CONTRA-003 UNRESOLVED: three intact disagreeing gc_012 records - certificate.json final_fitness 0.8041, gc_012 campaign result 0.8035, governance certificate 0.7864 (19:47); all T0-only; history adds no resolution (prior gate)"
  - "New coincidence disclosure (NOT provenance): 0.8035 also appears at data/archive/rea_generativity_trajectory_tensor.json (Trader-family individual coordinates.I) - simulation coordinate, unrelated to gc_012 fitness; same class as pre-T0 EMG zcr 0.106847 coincidence"
  - "Governance certificate self-hash mismatch: stored 409dbc2a... vs recomputed 0b1a6ab6... (byte-exact); FINAL certificate stated certificate_sha256 c5896ae4... vs whole-file 985e0073... (canonical-subset semantics undefined -> do NOT claim mismatch as truth, note as method-bound discrepancy)"
  - "Certification vs independent audit metric protocol divergence: laboratory gc_009/gc_010 use 1000 samples + joint gates; independent auditor uses 100 samples + single gates - same metric name, different computation"
  - "Wholesale certificate absence from history: all certificates first appear at T0 (prior gate); no historical certificate ever referenced execution records that genuinely existed (prior gate)"

unknowns:
  - "Whether a canonical subset hashing procedure exists for the governance / FINAL certificates (no specification found; would explain stored-vs-recomputed hashes)"
  - "Whether any persisted intermediate measurement corpus exists for gc_009/gc_010/gc_012 (none found in T0; metrics currently recompute from live governance state, not from saved measurements)"
  - "Whether Provenance.build records are ever persisted into the evidence database (code builds structs; persistence path of provenance records not located in this pass)"
  - "Meaning and provenance of archive config_hash 7a3244ead97d (12-hex prefix; unbound)"
  - "Whether execution_id and run_id uniqueness are enforced anywhere (no index located)"
  - "Exact semantics of the independent audit recomputation protocol vs the certification protocol (both live in code; not formalized)"

evidence_gaps:
  - "NO certificate-to-execution reference exists in any class (the single documented gap; closing it is the design objective)"
  - "Source identity absent from all execution records (no repo/commit/tree in ledger, packets, or physics rows)"
  - "Contract identity unbound (contract_hash null; no contract references execution or certificate)"
  - "Metric derivation absent from certificates (values inline, no calculation version, no input field list, no aggregation record)"
  - "No persisted measurement corpus (independent recomputation cannot replay the identical measurements)"
  - "No canonical hashing specification for certificates (self-hash semantics undefined across classes)"
  - "No materialized ProvenanceRecord persistently linked to any certificate (schema exists in code; binding layer absent)"

knowledge_changes: "NONE - reconnaissance adds disclosures and classifies edges; no existing ledger item is overturned by new evidence"
knowledge_debt_impact:
  - "KD-001/CONTRA-003: preserved unresolved; now explicitly distinguishes 'same execution/different representation' vs 'different executions' vs 'different metric calculations' vs 'historical artifact inconsistency' as the four candidate explanations the future architecture must discriminate via identities"
  - "KD-004: deepened (independent re-derivation currently impossible without measurement corpus + calculation version)"
  - "KD-005: touched (benchmark executability unchanged; not addressed in this design-only gate)"
  - "KD-010: deepened (governance/FINAL hash semantics unknown; canonicalization spec is a named evidence gap)"
  - "KD-011: unchanged"

research_question_impact:
  - "RQ-001 certificate truth: PARTIALLY ANSWERED (self-hash byte-integrity verified for certificate.json; semantic provenance for every class ABSENT)"
  - "RQ-004 independent re-derivation: UNANSWERED (no persisted corpus; auditor protocol differs; no evidence bundle defined until Artifact 4)"
  - "RQ-002 authority: PARTIALLY ANSWERED NEGATIVELY (meta-certificate self-named; no external anchor)"
  - "RQ-010 metrics blast radius: PARTIALLY ANSWERED (metric derivation is code-inline and unversioned; blast radius currently unbounded)"
  - "RQ-003 evolution execution: UNANSWERED (gc_012 execution identity absent)"
  - "RQ-009 audit attribution: UNANSWERED (no auditor identity or decision record binds to certificates)"
  - "RQ-006 record provenance: PARTIALLY ANSWERED NEGATIVELY (records exist but unbound; deletion indistinguishable from non-existence)"
  - "RQ-005 benchmark executability: UNANSWERED (design-only gate)"

gate: "PARTIAL PASS - the minimum provenance architecture is now characterized and defined (Artifacts 2-4); Pass criteria unmet only by evidence gaps that are themselves design-verification targets (metric corpus persistence, canonical hash spec, source/contract/execution identity in records, execution->result->metric->certificate edge, auditor protocol parity). No critical contradiction prevents the design; CONTRA-003 remains preserved as unresolved and is explicitly not reconciled by interpretation. Next authorized stage would be Council-decisional (implementation contract) - none is proposed in this gate."
implementation: "NONE"
remediation: "NONE"
certificate_regeneration: "NONE"
experiment_execution: "NONE"
commits: "NONE"
pushes: "NONE"
T0_modified: "NO"