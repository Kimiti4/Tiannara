# U1x-R2 Protocol — Unified Organism Re-Certification

**Class:** RE-CERTIFICATION + COMPOSITION. Auth-free (observational/dry-run).
**Purpose:** Prove CEL-1 and CEL-2 compose with the previously certified organism
loop without breaking causal integrity, governance, continuity, or epistemic provenance.

## NOT in scope (binding)
No new math domain. No CapabilityRegistry rewrite. No registry-architecture
expansion. No production-topology mutation. No new autonomous action capability.

## Baseline evidence (MUST be preserved; never modified)
- AE-004 → AE-010: RESOLVED
- CEL-1: PASS
- U1x: PASS
- CEL-2: PASS
- CEL-2 negative control: MATH_NOT_REGISTERED
- CEL-2 positive result: deterministic bayes_update/3 = 2.25

CEL-2 stays bounded: certified provider = constitutional_mathematics;
registry-wide dynamic discovery = NOT CERTIFIED. Do not silently upgrade.

## Primary objective
One causally continuous unified loop:
```text
C11 ingress → C1 → C2 → C3 → C4 → C5/C6 → C8 → C14
 → CEL-1 (ASC discovery/delegation)
 → CEL-2 (math discovery/delegation)
 → C9 ASC → C12 → C15 → C11 egress (dry-run)
```
The executive bridge MUST remain CEL registry discovery, not hardcoded routing.

## Critical composition requirement
Both paths through the SAME executive:
- objective requiring adaptation → registry query → ASC → C14 → delegation
- objective requiring math → registry query → constitutional_mathematics → C14 →
  delegation → Tiannara.Math.Probability.bayes_update/3

CEL-1 PASS ∧ CEL-2 PASS (independently) is NOT sufficient. Target:
CEL-1 + CEL-2 + organism loop = ONE causally connected execution.

## R1–R20 assertions
R1 C11 ingress trace exists.
R2 C1 consumes ingress causally.
R3 C2 produces/reads canonical reality state.
R4 C3 persists knowledge with lineage.
R5 C4 produces epistemic reasoning from that state.
R6 C5/C6 mathematical/research operation is real and deterministic.
R7 C8 consumes the resulting world/engineering context.
R8 C14 governance occurs before executive delegation.
R9 CEL-1 discovers+delegates to engineering/ASC provider.
R10 CEL-2 discovers+delegates to constitutional_mathematics.
R11 Both provider selections derived from registry queries.
R12 Neither provider selected via hardcoded shortcut.
R13 Real bayes_update/3 implementation executes.
R14 Mathematical result is deterministic.
R15 ASC path causally connected to the same executive trace.
R16 C12 operational under its certified topology.
R17 C15 preserves identity/causality/epistemic continuity.
R18 C11 egress remains governance-bounded dry-run.
R19 No unauthorized production mutation.
R20 Complete trace correlation reconstructible end-to-end.

## Rules
- **No-hardcoding:** no `objective→ASC`, `objective→constitutional_mathematics`,
  or `objective→Tiannara.Math.Probability` routing rules. Provider names may appear
  in assertions/verifiers, but the runtime selection must originate from
  registry_query/candidate_set evidence.
- **Governance order:** registry discovery BEFORE selection BEFORE governance
  BEFORE delegation. Governance after delegation = FAIL.
- **Mathematics:** real Tiannara.Math.Probability.bayes_update/3; deterministic;
  must match CEL-2 baseline (2.25). Discrepancy → record as finding, never normalize.
- **Continuity:** no synthetic reconstruction of missing lineage.
- **Homeostasis:** C12 bounded exactly as previously certified; no new fault/recovery.
- **Egress:** dry-run; no external production action.
- **Pollution:** no unauthorized registry/topology mutation, no unrelated
  registration, no unapproved external call, no state outside declared evidence paths.

## Verdict semantics
- **PASS:** all R1–R20 pass and the complete causal chain is reconstructible.
- **PARTIAL:** capabilities work but composition has an unresolved integration
  boundary, missing evidence, or conditional-topology limitation.
- **FAIL:** a required capability/path demonstrably violates its contract.
- **BLOCKED:** a required certified substrate/contract is unavailable.
Do not convert PARTIAL→PASS by weakening assertions.

## Bounded success claim (on PASS)
Establishes: "Tiannara's unified organism loop remains causally closed after adding
foundational mathematics as a CEL-discoverable capability, with CEL as the governed
executive bridge to both engineering/adaptation and mathematical reasoning."
Does NOT establish: full registry dynamism, unrestricted autonomy, complete math or
research coverage, security certification, complete self-awareness, or final
organism certification.

## Post-run order
Findings → evidence report → decision artifact → matrix update (append-only).
Do not modify prior U1x/CEL-1/CEL-2 records.
