defmodule Tiannara.WorldModel.Engineering.DesignRegistry do
  require Logger
  @doc "Get verification success rate"
  def get_verification_rate do
    {:ok, 0.0}
  end
  @doc "Get design success rate"
  def get_success_rate do
    {:ok, 0.0}
  end
end
